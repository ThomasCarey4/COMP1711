#include <stdio.h>

int main(){
    printf("The max size of an int16 is %ld\n", sizeof(int));
    printf("The max size of a float is %ld\n", sizeof(float));
    printf("The max size of a double is %ld\n", sizeof(double));
    printf("The max size of a char is %ld\n", sizeof(char));

    return 0;
}